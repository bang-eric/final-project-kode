const gray = document.getElementById('grayButton');
const normal = document.getElementById('normalButton');

function grayScale(element, opacity) {
    element.style.opacity = opacity;
};

function normalMode(element, opacity) {
    element.style.opacity = opacity;
};